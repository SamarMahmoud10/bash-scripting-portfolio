#!/bin/env bash 
case "$VARIABLE_TO_CHECK" in
    PATTERN_1)
        # Commands to run if pattern matches
        ;;
    PATTERN_2)
        # Commands to run if pattern matches
        ;;
    *)
        # Default case
        ;;
esac
