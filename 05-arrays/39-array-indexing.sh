#!/usr/bin/env bash
servers=("web-01" "web-02" "db-01" "monitoring")

# Get the first element (index 0)
echo "The first server is: ${servers[0]}"

# Get the second element (index 1)
echo "The web server is: ${servers[1]}"
