#! /bin/env bash 


#• Always start your script with an explicit shebang, for example: #!/usr/bin/env bash. This tells the
#kernel to run the file with Bash.
#• After creating a script, you must add the execute permission: chmod +x script.sh.
#• Run scripts by path (./script.sh) instead of relying on PATH. This avoids accidentally running the
#wrong file.